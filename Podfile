# Uncomment the next line to define a global platform for your project
# platform :ios, ’10.1’

target 'Erichood' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Erichood
  pod 'Alamofire', '~> 3.5'
  pod 'RealmSwift'
  target 'ErichoodTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ErichoodUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
